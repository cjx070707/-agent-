"""
文件：app/ops/rollback.py ｜ ★ 次生产级补丁（原文档无回滚矩阵）
说明依据：文档/次生产级灰度模拟可行性评估.md §4 缺口1；差别文档 §4

【做什么】
用一张「信号 → 动作」矩阵，说明灰度中出问题该关哪个能力，而不是先改业务代码。

【示例矩阵（讲解用，阈值数字可在真实压测后改）】

| 信号 | 优先动作（改 flag / 降流量） |
|---|---|
| 主任务超时率明显升高 | 关 ENABLE_FORK_PARALLEL 或降并发平台数；检查 300s 是否过紧 |
| P0 失败率升高（推超预算/违禁/泄露内部字段） | GLOBAL_KILL_SWITCH 或回退到上一 prompt/模型版本叙事 |
| Prompt Cache 命中率暴跌 | 检查是否错误压缩了断点前前缀；必要时临时关 ENABLE_CACHE_BREAKPOINT |
| 召回空结果率升高 / Tower 异常 | 关 ENABLE_THREE_TOWER，叙事上退回简化检索 + 原文 13-1 组件降级 |
| 记忆注入导致明显误伤（过度过滤） | 关 ENABLE_MEMORY_INJECT，保留 Store 写入但暂停注入 |

【解决的问题】
只有组件内 try/except 降级，没有「放量层面的急停」。回滚矩阵让实习生也能讲清：
灰度不是看着看板干着急，而是预先约定动作。

【和其他文件的连接】
- 读 metrics.py 的口径；写 flags.py 的开关状态（概念上）。
- 与原文 13-1「组件降级」互补：那边保证单次请求还能返回结构；这里决定
  「还要不要继续给更多用户开这个能力」。

【面试怎么说】
「回滚优先拨 flag，不优先热修 Agent 逻辑。原文给了工具内兜底；ops/rollback
是我们补的放量层急停表。」
"""

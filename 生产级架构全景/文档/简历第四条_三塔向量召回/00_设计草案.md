# 简历第四条 · 三塔向量召回：设计草案

> 状态：**已代为定稿并落地**（过面试标准）。正式材料见 `01` / `03`。

## 定稿决策

| 项 | 决定 |
|---|---|
| 体例 | 01 + 03 + `towers.py` / `ann.py` + `demo_tri_tower_recall.py` |
| 运行时主叙事 | 跟第 11 章：`item_search` 内双通道 ANN（语义始终开；有 `user_id` 开个性化）→ 并集去重 → Top-K |
| 原理补强 | 跟 04-0：为何三塔、双通道目标解耦；跟 04-1：Faiss vs OpenSearch |
| +22% / +35% | **自拟可辩护**（原文无该对比表，见 `01` §5）；面试主动说明 |
| 日程 | 约 6 天，不训真模型、不上真 Faiss |
| 深度 | 三塔分工、双通道、ANN/后过滤、与 Agent/Store/Rerank 边界——够用；CPT/SFT/DPO 只知阶梯存在 |

原文：`globex/04_0` `04_1` `04_2` + `11item_search_fork_reproduction.md`。

# 简历内容学习与面试准备 —— 复盘笔记

> 用途：记录每一轮"我先讲 → 被拷问/纠正 → 补概念"过程中，哪里讲错/讲漏了，
> 以及最终沉淀下来的"精华理解"。目标不是记流水账，是给考前冲刺用的最终答案。
>
> 关联材料：
> - 原始设计文档：`globex/`（只读，第一手依据）
> - 复刻代码：`globex-agent/`（你已实现的部分，能直接指着代码讲）
> - `生产级架构全景/`：模拟完整生产级项目的文件树，每个文件内部是结构化讲解
>   （做什么/解决的问题/输入输出/连接关系/技术栈），不要求能跑通，重点覆盖
>   简历四条对应的文件；`app/ops/` 是次生产级灰度补丁（非原文档）。
> - `生产级架构全景/文档/`：`00_项目地图.md`（先读）+ 可行性评估 +《原文档与次生产级差别》。
> - `生产级架构全景/demos/demo_*.py`：不跑真实业务逻辑，只抽出"核心机制"
>   写成可运行的最小示例，用来验证/证明某个技术判断，不是最终工程代码。

---

## 模块① 多 Agent 架构（主 Agent + Fork 子 Agent）

### 你的第一版口述（原话，用于对比进步）

> "多 Agent 架构选型和落地，这个项目我们选择的是主Agent + forkSubagent的架构，
> 符合多平台搜索是基本同类型任务的性质，解决单Agent的上下文爆炸问题，并行返回
> 结构化参数，对主Loop进行最多30轮的迭代限制，跨平台的并行检索延迟较串行降低
> 百分之65（这里的数据还是并行其实我基本都不知道具体是怎么做的）。"

### 讲对的部分 ✅

- fork 的适用场景抓住了一个关键点：**多平台检索是"同类型、可并行"的任务**，
  这正是文档里"同质子 AgentLoop"（不是异构 worker，是复制出来的克隆）的核心
  适用条件之一。
- "解决单 Agent 上下文爆炸"——方向对，这是 fork 三大理由之一（上下文隔离）。
- "30 轮迭代限制"这个数字记对了，代码里 `MAIN_AGENT_RECURSION_LIMIT = 30`
  （`app/agent/main_agent.py`）跟文档 `globex/14_extract.md` 的
  `MAIN_AGENT_MAX_ITERATIONS = 30` 完全一致。

### 讲漏/讲错的部分 ⚠️

1. **"并行返回结构化参数"这个说法不准确。**
   子 Agent 不是"返回参数"，而是执行完自己的一整轮 Think→Act→Observe 后，
   把最终结果压缩成一段**字符串 Observation** 返回给主 Agent（看
   `dispatch_tool.py` 第 36 行 `return str(result["messages"][-1].content)`）。
   "结构化"体现在 Prompt 里要求子 Agent 用紧凑 JSON 文本表达结果，但传输载体
   始终是字符串，不是真正的结构化对象——这也是文档里明确点出的一个稳定性风险点
   （字符串 Observation 的合流稳定性要重点验证）。

2. **漏了"300 秒超时"，只提到"30 轮"。**
   文档里这两个限制是配套出现的：
   `MAIN_AGENT_MAX_ITERATIONS = 30` + `MAIN_AGENT_TIMEOUT_SEC = 300`。
   30 轮防的是"来回调工具但不收敛"（逻辑死循环），300s 防的是"某一步卡住不返回"
   （比如网络挂了、下游服务没响应）。两者防的故障模式不同，只说一个会显得考虑不全。
   （你自己代码里目前只做了 30 轮的 `recursion_limit`，300s 超时还没实现——
   这是一个"文档要求 vs 当前代码"的真实缺口，面试如果问起来要如实说清楚。）

3. **完全没提到"防失控四件套"里的另外两个：fork 深度上限、重复调用检测/结果截断。**
   - fork 深度上限 2 层（`fork_guard.py`）：防子 Agent 内部再调用
     `dispatch_tool` 无限递归下去。
   - 同一工具单次 run 最多调 4 次、超限拦截但不杀死整个流程
     （`middleware.py` 的 `ToolCallLimitMiddleware`）：防模型死循环调同一个工具。
   - 单个工具结果超过 16000 字符自动截断（`truncate_long_tool_result`）：
     防某个工具一次返回超长结果把上下文撑爆。
   面试官如果问"只靠 30 轮限制够吗"，正确答案是"不够，30 轮/300s 是兜底，
   还需要深度、重复调用、结果长度三层配合，是纵深防御（defense in depth）思路"。

4. **最大的空白——"并行到底怎么实现的"，你自己也承认不知道。这是本轮补的重点，见下方"精华理解"。**

### 精华理解（最终定稿，考前直接看这一段）

**Q: 主 Agent 和子 Agent 是什么关系？**

不是"分工不同的角色"，是**同一份代码的克隆**：用一样的 LLM、一样的
`system_prompt`、一样的 `FULL_TOOL_SET`（包括 `dispatch_tool` 自己），
唯一区别是 `thread_id` 和 checkpoint 独立（子 Agent 用
`sub-{uuid8}-d{depth}`），所以子 Agent 的多轮对话历史不会混进主 Agent 的
State，反过来也一样——这就是"上下文隔离"的字面意思。

**Q: 什么时候该 fork，什么时候不该？**（三个判断依据，缺一不可但满足一个就够）

- 能不能并行（多平台、多品类可以同时查）
- 需不需要上下文隔离（子任务信息量大，不该塞进主线）
- 调用链会不会变深（子任务本身要多步 Think）

反例：`PriceCompare`、`ShippingCalc` 是候选合流后的**确定性计算**，
不需要"思考"，也没有并行检索的必要，所以不 fork，主 Agent 直接调用普通工具。

**Q: "跨平台并行检索延迟降低 65%"这句话，技术上到底是怎么发生的？**（重点，之前的空白）

链路是：

```text
主 Agent 的 LLM 在一次 Think 里，判断要查 4 个平台
  -> 一次性在同一条 AIMessage 里产出 4 个 tool_calls（都是 dispatch_tool，
     args 里 platform 不同）
  -> LangGraph 的 ToolNode（框架内部，不是我们写的代码）拿到这 4 个
     tool_calls 之后，不是 for 循环一个个 await 执行，而是用
     ContextThreadPoolExecutor.map() 把它们丢进一个线程池并发跑
  -> 每个线程各自跑一次 dispatch_tool -> build_agent().invoke(...)
     （子 Agent 自己的 Think -> Act -> Observe 循环），这里面大部分时间
     花在等 LLM API 的网络响应，是 I/O 等待，不是 CPU 计算
  -> Python 的 GIL 在网络 I/O 等待期间会释放，所以 4 个线程的"等待时间"
     可以重叠，总耗时约等于最慢那个平台的耗时，而不是 4 个平台耗时的总和
```

我写了一个不跑真实 LLM 的最小验证脚本（`生产级架构全景/demos/demo_parallel_fork.py`），
用 `sleep` 模拟 4 个平台的网络等待时间（3s/2s/2.5s/1.5s），实测结果：

```text
串行总耗时: 9.02s
并行(线程池)总耗时: 3.00s
延迟降低: 66.7%
```

这和简历上"65%"是同一个数量级，说明这个数字很可能就是这样估算出来的——
**"串行=各平台耗时之和，并行≈最慢平台耗时"**，不是玄学，是线程池并发 I/O
等待的直接结果。

**Q: 为什么 `fork_guard.py` 要用 `ContextVar`，而不是普通变量或者传参？为什么普通
`ThreadPoolExecutor` 会让这套机制失效？**（加分项，面试官深挖并发细节时的储备）

`fork_guard.py` 用 `ContextVar` 记录"当前 fork 深度"，好处是不用在每一层函数调用
里显式传 `depth` 参数，靠 Python 的上下文变量自动"跟着调用链走"。但普通线程池
（标准库 `ThreadPoolExecutor`）新开线程时**不会自动带着调用方当时的 ContextVar
值过去**（新线程默认拿到的是初始默认值）。LangChain 专门写了一个
`ContextThreadPoolExecutor`，在 `submit`/`map` 时显式
`copy_context()` 后在子线程里 `context.run(...)`，才让子线程"继承"了父线程
当时的 `fork_depth` 值。我在 demo 脚本里直接对比验证：

```text
普通 ThreadPoolExecutor 里子线程看到的 fork_depth: [0, 0, 0, 0]  <- 丢了
ContextThreadPoolExecutor 里子线程看到的 fork_depth: [1, 1, 1, 1]  <- 对的
```

这说明 LangGraph 底层选用 `ContextThreadPoolExecutor` 不是随便选的库，是因为
Agent 框架本身（以及我们自己写的 `fork_guard`）大量依赖 `ContextVar` 做跨函数
透明传参，必须要有能正确传播 context 的执行器配合，否则并发场景下 fork 深度
计数会全部失真，防失控机制直接失效。

### 真实实习场景 vs 复刻/文档里的理想架构

- 作为实习生，你大概率**不会去写 LangGraph ToolNode 内部的线程池调度逻辑**
  （这是框架自带的），但你**会负责**：
  - 设计和调整 fork 触发条件、深度上限、超时阈值、重复调用上限这些"防失控参数"
    （这些数字往往是压测/试错调出来的，不是理论算出来的）
  - 写 `dispatch_tool` 这一层封装，决定子 Agent 拿到什么样的输入（demands
    怎么拼）、返回什么样的输出格式
  - 做压测/对比实验，量化"fork 前 vs fork 后"的延迟差异，这就是"65%"这类数字
    的来源——**如果面试官问你"这个数字怎么测出来的"，合理的答法是"固定一条
    跨平台 query，分别在禁用/启用并行 fork 的情况下跑多次取平均耗时对比"**，
    不要说"文档里就是这么写的"。

---

## 待续模块（后续每轮追加）

- 模块②：Cache Breakpoint 上下文压缩
- 模块③：长期记忆 Store（含 72% 命中率口径待厘清）
- 模块④：三塔向量召回（含 22%/35% 提升口径待厘清）
